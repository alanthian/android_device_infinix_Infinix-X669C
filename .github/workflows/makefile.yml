name: Makefile CI

on:
  push:
    branches: [ "vnd_x669c_h6127-user-12-SP1A.210812.016-290007-release-keys" ]
  pull_request:
    branches: [ "vnd_x669c_h6127-user-12-SP1A.210812.016-290007-release-keys" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: configure
      run: ./configure

    - name: Install dependencies
      run: make

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
